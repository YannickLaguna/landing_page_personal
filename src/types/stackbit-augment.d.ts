import '*/generated';
declare module '@/types/generated' {
    // Extiende el tipo en vez de redeclarar la interfaz para evitar duplicados
    // Puedes agregar propiedades adicionales aquí si es necesario
    // interface GeneratedContentObjectMetadata {
    //     sourceFilePath?: string;
    // }
} 